import React from "react";

const Sidebar = () => {
  return (
    <div className="w-[4.5rem] flex flex-col items-center py-5">
      <div className="rounded-[1.4rem] flex items-center justify-center font-bold text-slate-600 w-[2.8rem] h-[2.8rem] bg-slate-100 mb-2 hover:rounded-[1rem] cursor-pointer transition-all">
        bs
      </div>
      <div className="rounded-[1.4rem] flex items-center justify-center font-bold text-slate-600 w-[2.8rem] h-[2.8rem] bg-slate-100 mb-2 hover:rounded-[1rem] cursor-pointer transition-all">
        bs
      </div>
      <div className="rounded-[1.4rem] flex items-center justify-center font-bold text-slate-600 w-[2.8rem] h-[2.8rem] bg-slate-100 mb-2 hover:rounded-[1rem] cursor-pointer transition-all">
        bs
      </div>
    </div>
  );
};

export default Sidebar;
